mod path;

pub use path::{normalize_path, ensure_directory_exists};

/// Utility functions for ArtiGit
pub struct Utils;

impl Utils {
    /// Create a new utils instance (placeholder)
    pub fn new() -> Self {
        Self
    }
}